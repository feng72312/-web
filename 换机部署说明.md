# 紫云：另一台 Linux 电脑部署说明

这份说明对应私有仓库 [zy-linux](https://gitee.com/lqf8121/zy-linux)。仓库里已经带上本机 `.env`、DeepSeek 密钥和 RAG 向量库分卷，克隆后按下面做即可启动。

仓库请保持**私有**。密钥在 `code/backend/.env` 里，不要改成公开仓库。

## 1. 环境要求

- Ubuntu / Debian 一类 Linux
- Python 3.11（本机就是 3.11）
- Node.js 20+（前端 `npm`）
- `git`、`curl`、`gzip`
- 有 NVIDIA GPU 时 RAG 会更快；没有 GPU 也能跑，把 `RAG_DEVICE=cpu` 即可

克隆体积大约几百 MB，主要是 Chroma 分卷。

## 2. 克隆

本机已配好 Gitee SSH 时：

```bash
git clone git@gitee.com:lqf8121/zy-linux.git
cd zy-linux
```

HTTPS：

```bash
git clone https://gitee.com/lqf8121/zy-linux.git
cd zy-linux
```

密钥文件克隆下来就会有，不必再复制：

- `code/backend/.env`（DeepSeek、Cursor、后台密码、RAG 地址）
- `code/frontend/.env`
- `code/frontend/.env.local`

## 3. 还原 RAG 向量库

Gitee 免费仓库单文件不能超过 100MB，所以 `chroma.sqlite3` 被压成 5 个分卷。集合目录已经在仓库里，只要还原主库：

```bash
bash code/rag/restore-chroma.sh
```

成功后应出现 `code/rag/data/chroma/chroma.sqlite3`（约 953MB）。已经存在则脚本会直接退出，可重复执行。

## 4. 安装依赖

在仓库根目录执行。

后端：

```bash
python3.11 -m venv code/backend/.venv
code/backend/.venv/bin/pip install -U pip
code/backend/.venv/bin/pip install -r code/backend/requirements.txt
```

RAG（有 GPU 时先装 CUDA 版 PyTorch，再装其余依赖）：

```bash
python3.11 -m venv code/rag/.venv
code/rag/.venv/bin/pip install -U pip
# GPU 示例（按显卡改 cu124 / cu121）；没有 GPU 就装 CPU 版
code/rag/.venv/bin/pip install torch --index-url https://download.pytorch.org/whl/cu124
code/rag/.venv/bin/pip install -r code/rag/requirements-server.txt
```

没有 GPU：

```bash
code/rag/.venv/bin/pip install torch --index-url https://download.pytorch.org/whl/cpu
code/rag/.venv/bin/pip install -r code/rag/requirements-server.txt
export RAG_DEVICE=cpu
```

前端：

```bash
cd code/frontend
npm install
cd ../..
```

`node_modules` 和两个 `.venv` 都没有进仓库，换机必须重装。

## 5. 启动

三个端口必须空闲：`8100`（RAG）、`8002`（后端）、`5173`（前端）。

```bash
cd code
./start-all.sh
```

脚本会先起 RAG，等健康检查通过后再起后端和前端。日志在 `code/logs/`。

单独启动：

```bash
./start-rag.sh
./start-backend.sh
./start-frontend.sh
```

前端脚本会先 `npm run build` 再 `preview`。本机开发也可用：

```bash
cd frontend
npm run dev
```

## 6. 访问与检查

| 服务 | 地址 |
| --- | --- |
| 前端 | http://127.0.0.1:5173 |
| 后端健康 | http://127.0.0.1:8002/health |
| RAG 健康 | http://127.0.0.1:8100/health |

局域网访问用这台电脑的 IP 替换 `127.0.0.1`，端口仍是 `5173`。Vite 已监听 `0.0.0.0`，`/api` 会反代到本机 `8002`。

快速检查：

```bash
curl -sf http://127.0.0.1:8100/health && echo rag-ok
curl -sf http://127.0.0.1:8002/health && echo backend-ok
curl -sf http://127.0.0.1:5173 >/dev/null && echo frontend-ok
```

## 7. 和这台 ThinkStation 长期部署的区别

`code/deploy/local/` 里的 systemd / nginx 是给 **user-ThinkStation-PX** 写的，服务文件里写死了：

`/home/feng/liqingfeng/ZY`

另一台电脑如果路径不同，不要直接 `systemctl --user enable` 那些 unit，先改 `WorkingDirectory` 和 `ExecStart` 里的路径，或继续用上面的 `start-all.sh`。

ThinkStation 本机文档：`code/deploy/local/README.md`。

## 8. 仓库里有什么、没有什么

已包含：

- 前后端、RAG 源码
- `.env` 与 DeepSeek 密钥
- Chroma 集合文件 + sqlite 分卷
- 本说明和本地部署模板

未包含（换机后重建或另拷）：

- `code/backend/.venv`、`code/rag/.venv`
- `code/frontend/node_modules`
- `数据库/`、`backups/`、`未入库古籍/`（原始典籍；已入库内容在 Chroma 里）
- 运行日志、`beike/` 嵌套项目

## 9. 常见问题

**`[rag] missing venv` / `[backend] missing venv`**  
还没建虚拟环境，回到第 4 步。

**`restore-chroma.sh` 提示缺分卷**  
确认 `code/rag/data/chroma-parts/chroma.sqlite3.gz.part-00` 到 `part-04` 都在。重新 `git pull` 后再跑脚本。

**端口被占用**  
`start-all.sh` 发现 8100 / 8002 / 5173 被占用会直接退出，不会杀已有进程。用 `ss -tlnp | grep -E '8100|8002|5173'` 查看。

**RAG 很慢或 CUDA 报错**  
`export RAG_DEVICE=cpu` 后再执行 `./start-rag.sh`。

**AI 对话失败**  
看 `code/backend/.env` 里是否还有 `BAZI_DEEPSEEK_API_KEY`，以及本机能否访问 `https://api.deepseek.com`。

**前端能开、接口 502**  
先确认 `8002` 健康。前端 preview 把 `/api` 转到 `127.0.0.1:8002`。

**从仓库更新**

```bash
git pull
bash code/rag/restore-chroma.sh
```

`.env` 若被覆盖，用本机备份比对后再启动。

## 10. 架构阅读

源码学习指南：`code/docs/项目架构与源码学习指南.md`
