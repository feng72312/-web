# ZY 开发思路

紫云命理天文馆。把确定性术数计算做成可复现的引擎，把不确定的解读交给典籍检索与大模型，用大赛真题检验对错。

生成日期：2026-09-04。更细的原理见 `report/命理师大赛/项目原理.md`，变更流程见 `openspec/`。

## 要解决什么

算命软件常见两条弯路：整段交给模型编，或只做排盘表格没有说法。ZY 拆成三层，各层可单独坏、单独换。

- **盘**：四柱、大运、紫微十二宫、六爻卦象等，同一输入必须同一输出。
- **判**：规则图谱、多裁判仲裁、分层证据。典籍优先，命例为辅，模型不得改写盘面事实。
- **说**：RAG 抽原文，DeepSeek 组织白话或专业文风。Key 只存在后端，前端只打本站 API。

没有 Key、没有 RAG 时，排盘和分析模块仍能跑。解读失败有降级，不挡主路径。

## 系统怎么拼

前端 React + Vite。后端 FastAPI。RAG 是独立进程（Chroma + 向量模型，默认 `127.0.0.1:8100`）。LLM 走 DeepSeek 云端。

```
浏览器 ──► 前端 :5173
              │
              ▼
         后端 :8002 ──► PaipanEngine / 各术数引擎
              │     ──► AnalysisRegistry / JudgementChain
              │     ──► ChatOrchestrator ──► DeepSeek
              └──► RAG :8100 ──► 数据库/*.txt 索引
```

当前主场是局域网 Ubuntu（`172.16.7.144`），脚本在 `code/start-all.sh`。CloudBase 登录链路已弃用。临时外网用 Cloudflare Quick Tunnel，并靠访问口令挡计费接口。GPU 给 RAG 嵌入和重排序，与同机其他训练任务共存。

## 前端怎么长

产品壳三页：首页、预测模块、AI 对话。模块按术数门类分 Tab（八字、紫微、六爻、梅花、奇门、大六壬、风水、塔罗、星命、实用工具），工作台皮肤共用一套朱砂/金/宣纸 token，各门只换主题色。

八字是主路径，也是判盘链、流式解读、证据展示最完整的一门。其他门先保证起盘与非流式解读，再按同一套路补判与流式。分析区块走注册表：后端出一个 section，前端挂一块 UI，不把十神、五行、大运写死在页面里。

## 知识怎么进系统

`数据库/` 按门类放原典。八字侧做成三库：源文件清单、规则图谱、命例经验。索引进 Chroma，检索带权威层级和能否直接下判的元数据。

判盘不是「检索一段就当结论」。`BaziJudgementChain` 多个 Judge 各自给证据和倾向，再仲裁。大赛题按主题分流（感情/健康走全链，应期/静态走独立通道），避免一种 prompt 打全部题型。

## 对错怎么量

全球命理师大赛 2021–2025 共 200 道四选一，是主评测环，不是附属脚本。

改规则或通道时：相关单测 → 子集或 val 冒烟 → 阶段边界再打全量 200。报告落在 `report/命理师大赛/`。不用假设备、不造假题。准确率是阶段门禁，不是上线装饰。

紫微、六爻有各自的「最强方案」文档，思路相同：先引擎可复现，再规则，再用题说话。

## 改动怎么进仓库

非琐碎需求走 OpenSpec：`openspec/changes/<id>/` 里写提案、设计、delta spec、任务清单，校验通过再改代码。做完归档，不把半成品规格留在主 specs 里充数。

约定：最小 diff，跟着现有模块切分走（`core/paipan`、`core/judgement`、`core/knowledge`、`benchmark`）。功能分支开发，用户点头才提交。密钥、口令、`.env`、向量库和备份不进 Git。

## 明确不做什么

- 前端直连模型厂商。
- 用模型输出覆盖排盘数字或改写证据原文。
- 为了演示关掉判盘链、用假检索顶真典籍。
- 一门术数尚未起盘稳定，就先上花活 UI 或融合解读。

## 以后往哪补

同一骨架往外长：缺的门先补确定性引擎，再挂 RAG 与判，最后才是流式和视觉。对外演示优先把「进馆 / 起盘 / 揭晓」三处做干净，而不是再铺一层产品卖点页。部署若要从脚本变成常驻，再启用 `code/deploy/local` 的 systemd / Nginx，与现在的 `start-*.sh` 是同一套端口，不是另一套架构。
