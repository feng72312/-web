# 阶段12-子平真诠格局第三批记录

## 阶段目标

扩展《子平真诠》格局规则图谱第三批, 补齐杂格/外格/从格等 15 类特殊格局节点, 接入 GejuJudge 判盘链, 前端按成败救应/月令格局/杂格外格分组展示 ruleIds, 并验证 RAG baseline 不退化喵

## 本阶段变更

### 抽取脚本

- 扩展 `code/knowledge/scripts/extract_geju_ziping.py` 第三批 `SPECIAL_SECTIONS`
- 新增 15 个杂格外格节点: `geju:special:{waige|zhuanwang|zagai|quzhi|huaji|daochong|chaoyang|helu|congcai|congsha|jinglan|xinghe|yaohe|jishen_po|xionshen_cheng}`
- geju 节点 20 -> 35 (含第二批 4 aspect + 8 core + 8 yun + 15 special)

### 后端判盘

- `keys.py` 增加 `build_geju_special_key`
- `store.py` geju 索引支持 `geju_special` 分类键
- `GejuJudge` 挂载:
  - `taboo` 成败救应节点
  - 杂格外格边界: `zagai`, `waige`, `jishen_po`, `xionshen_cheng`
  - 五行偏旺 >= 42% 时附加: `quzhi`, `zhuanwang`

### 前端

- `BaziJudgementPanel.tsx` 对 geju 裁判意见按三组展示 ruleIds:
  - 成败救应 (`geju:aspect:*`)
  - 月令格局 (`geju:month:*`)
  - 杂格外格 (`geju:special:*`)
- `bazi-visual-demo.css` 增加 `.bazi-rule-groups` 样式

### 图谱编译

- `compile_graph.py` 后 nodeCount=267, geju=35

## 验证结果

| 验证项 | 结果 |
|--------|------|
| extract_geju_ziping | 35 nodes |
| compile_graph | nodeCount=267, geju=35 |
| pytest (judgement/geju) | 8 passed |
| RAG val baseline 20题 | coverageRate=1.0, caseOverreach=0 |
| geju ruleIds 每题 | 11-12 条 (含 special 与 taboo) |

输出文件: `report/八字判盘最强方案/val_judgement_baseline_rag_geju_batch3.json`

## 对比第二批 baseline

- coverageRate: 1.0 -> 1.0 (保持)
- caseOverreach: 0 -> 0 (保持)
- geju ruleIds 每题: 5 条 -> 11-12 条
- primaryEvidenceCount: 1-2 -> 1-2 (保持)

## 仍待深化

1. 从财/从煞/化气/倒冲等具体杂格仍依赖五行偏旺粗启发式, 尚未按日主强弱与透干精确触发
2. `huaji`, `congcai`, `congsha` 等节点已入库但默认链未按命局条件挂载
3. contest eval 尚未用新增 ruleIds 做准确率回归
4. 证据链 evidenceChain 可按 geju 分组与 special 节点联动展示原文

## 状态结论

**完成**. 子平真诠格局第三批已落地, 图谱 geju=35, 判盘链与前端分组展示已接通, RAG baseline 指标未退化喵
