# 阶段10-文件实体整理与最终复核记录

## 阶段目标

按当前三库分离和 S/A/B/C/D 分类结果, 将 `数据库/01八字命理` 内的真实源文件实际移动到对应分类目录, 并复核整理后 manifest、RAG、规则抽取和方案剩余缺口喵

## 文件整理结果

`数据库/01八字命理` 现已按权威分层整理为以下目录喵

| 目录 | 文件数 | 用途 |
|------|--------|------|
| `S_主裁经典` | 9 | 调候、格局、气势、十神、岁运主裁经典 |
| `A_辅助经典` | 13 | 辅助古籍与旁证 |
| `B_现代解释` | 33 | 现代解释、教材、说明材料 |
| `C_命例经验` | 69 | 命例、详批、答疑、实战经验 |
| `D_低信排除` | 19 | 讲义、网络整理、低信材料 |

未复制副本, 采用移动方式整理, 避免 RAG 递归索引重复收录喵

## 新增和更新文件

- 新增 `code/knowledge/scripts/organize_01_sources_by_classification.py`
- 更新 `code/knowledge/scripts/reconcile_01_sources.py`, 改为按 `01八字命理` 内相对路径对账
- 更新五个规则抽取脚本默认源路径, 指向 `S_主裁经典`
- 更新 `数据库/01八字命理/_文件分类清单.md`
- 更新 `report/八字判盘最强方案/01八字命理文件分类清单.md`
- 新增整理日志 `report/八字判盘最强方案/01八字命理文件整理记录.json`
- 新增整理后对账 `report/八字判盘最强方案/reconcile_01_after_organize.json`

## 验证结果

| 验证项 | 结果 |
|--------|------|
| 文件移动 | 143 个源文件移动到分类目录 |
| classify_01_sources | 143 文件, S=9/A=13/B=33/C=69/D=19 |
| reconcile_01_sources | diskFiles=143, manifestFiles=143, aligned=true |
| 规则抽取 | tiaohou=100, geju=8, qishi=16, shishen=30, suiyun=65 |
| compile_graph | nodeCount=240 |
| 后端测试 | 10 passed |
| RAG 重建 | 已完成, 143 文件 / 12354 chunks |
| metadata spot check | 通过, missingRequiredMetadata=0 |
| DeepSeek-only smoke | `cursor_sdk` 已无导入, FastAPI `/health` 与 `/api/v1/paipan/judgement` smoke 通过 |
| RAG val baseline | val 20 题, coverageRate=1.0, caseOverreach=0, primaryEvidenceCount=1-3 |

## 整理后的重要行为变化

manifest 的 `sourceFile` 现在包含分类目录, 例如 `S_主裁经典/穷通宝鉴-明-余春台.txt`, 这样人可以直接按目录查看, RAG 也可以保留分类路径喵

`source_manifest_loader.lookup_file_meta()` 已能通过文件名后缀匹配 manifest, 因此 `build_index.py` 在递归读取子目录时仍能拿到正确 metadata 喵

规则抽取脚本已改为读取 `S_主裁经典` 下的主裁经典, 后续重建规则图谱不会因为文件移动而找不到源文件喵

## 最终方案遗漏复核

### 已完成主链路

1. 三库分离和文件实体分类已经落地喵
2. RAG chunk metadata 已支持 authorityTier、evidenceRole、libraryRole、canJudge 喵
3. 规则图谱第一批 240 节点已编译喵
4. 判盘裁判器已可输出 ruleIds 和 evidence request 喵
5. 命例材料已保持 caseReference, 不可主裁喵
6. 前端已能展示判盘链、ruleIds 和四层证据喵
7. val 20 题离线判盘链覆盖率为 100% 喵

### 仍未完成或下一轮优先项

1. 杂格、外格、从格等《子平真诠》后半仍待第三批抽取喵
2. AI 解读段落尚未强制绑定 `ruleIdRefs`, 前端报告还可继续深化喵
3. case schema 仍偏浅, 后续可补 eventYear、luckTrigger、doNotApplyReason 喵
4. 云端部署按当前要求跳过, 上线前仍需补 CloudBase 和线上 smoke 喵

## 本轮 DeepSeek-only 与 RAG baseline 补充

按最新要求, `cursor_sdk` 已完全从导入链路移除, 后端启动阶段不再初始化 Cursor service, 聊天与解释链路只保留 DeepSeek provider 喵

本轮还修正了 `code/rag/build_index.py` 的索引 metadata 写入逻辑, `classic` 字段优先使用 manifest 的标准经典名, 确保 `classicWhitelist` 能过滤到 `穷通宝鉴`、`滴天髓` 等主裁经典喵

`code/backend/app/benchmark/contest8_benchmark_meta.py` 的 `caseOverreach` 指标已改为“存在命例参考且没有主裁证据”才算越界, 与“命例只能旁证、不能压过经典”的方案目标一致喵

最终输出文件为 `report/八字判盘最强方案/val_judgement_baseline_rag.json`, 汇总结果为 total=20, coverageOk=20, coverageRate=1.0, caseOverreach=0, primaryEvidenceCount 最小值为 1, 最大值为 3 喵

## 状态结论

**完成**. `数据库/01八字命理` 文件已按分类结果完成实体整理, 分类清单、manifest、对账、规则抽取、后端测试、RAG 重建、metadata 抽测、DeepSeek-only smoke 和 `--rag` 版 val baseline 均已验证; 剩余任务已收敛为规则细化、报告绑定和上线前云端验证喵
