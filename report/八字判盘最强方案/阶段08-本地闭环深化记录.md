# 阶段08-本地闭环深化记录

## 阶段目标

在不上云前提下, 补齐阶段07遗留的本地验证: RAG 索引重建、judgement smoke、val 判盘链基线、metadata 抽测能力喵

## 输入资料

- 阶段07下一轮优化项
- Python 3.10 环境 (`py -3.10`)
- 真实大赛 val split: `命理师大赛试题/data/contest8_2024.json`

## 改动范围

- `code/rag/build_index.py`: 关闭 anonymized_telemetry
- 新增 `code/rag/spot_check_metadata.py`
- 新增 `code/backend/scripts/smoke_judgement_local.py`
- 新增 `code/backend/scripts/val_judgement_baseline_local.py`

## 核心实现

### 1. RAG 索引重建(已完成)

```bash
py -3.10 code/rag/build_index.py --category 01
```

- Python 3.8 因 posthog TypedDict 报错无法建库
- Python 3.10 + chromadb 0.5.23 可正常跑通
- 本轮删除旧 chroma 后全量重建 01八字命理(143 文件), 已完成 12354 chunks

### 2. 本地 judgement smoke(不依赖 FastAPI/cursor_sdk)

```bash
cd code/backend
python scripts/smoke_judgement_local.py --output ../../report/八字判盘最强方案/smoke_judgement_local.json
```

结果摘要:
- stepCount=8, opinionCount=6, evidenceChainCount=5
- lookupKeys: tiaohou/geju/qishi/shishen/suiyun
- ruleIds 命中真实图谱节点(含 tiaohou/geju/qishi/shishen/suiyun)
- caseReferenceCount=2, canJudge=false

### 3. val 判盘链离线基线(无 LLM)

```bash
cd code/backend
python scripts/val_judgement_baseline_local.py --split val --limit 20 --output ../../report/八字判盘最强方案/val_judgement_baseline_local.json
```

结果摘要:
- total=20, coverageOk=20, coverageRate=1.0
- 五类裁判意见(role)全覆盖
- caseOverreach=20: 因离线未开 RAG, primaryEvidence=0 仅有 caseReference(符合监控预期)
- 开 RAG 后应重跑: `python scripts/val_judgement_baseline_local.py --split val --limit 20 --rag`

### 4. metadata 抽测脚本(已完成)

```bash
cd code/rag
py -3.10 spot_check_metadata.py --query "甲木寅月 调候 用神" --output ../../report/八字判盘最强方案/rag_spot_check.json
```

抽测结果: hitCount=3, missingRequiredMetadata=0, 返回均为 S / tiaohou_judge / judge_library / canJudge=true / primaryEvidence

## 真实数据处理

- smoke 使用 PaipanEngine 真实排盘 1990-03-15
- val baseline 使用大赛 2024 年真题 20 道
- 未使用模拟数据

## 验证命令

```bash
py -3.10 code/rag/build_index.py --category 01
python code/backend/scripts/smoke_judgement_local.py
python code/backend/scripts/val_judgement_baseline_local.py --split val --limit 20
py -3.10 code/rag/spot_check_metadata.py --query "穷通宝鉴 甲木 寅月"
cd code/backend
python -m pytest tests/test_bazi_evidence_report.py tests/test_bazi_judgement_chain.py tests/test_rule_graph_judges.py tests/test_case_experience_store.py -q
```

## 验证结果

| 项目 | 状态 |
|------|------|
| smoke_judgement_local | 通过 |
| val baseline 20 题 | coverage 100% |
| pytest 核心项 | 11 passed |
| RAG rebuild | 完成, 143 文件 / 12354 chunks |
| metadata spot check | 通过, missingRequiredMetadata=0 |
| 云端部署 | **按用户要求跳过** |

## 偏差与风险

1. 离线 baseline 的 caseOverreach=100% 不代表生产问题, 仅反映未启用 RAG
2. FastAPI 全应用 smoke 仍受 cursor_sdk 依赖影响, 已用独立脚本绕过
3. geju 节点仍仅 8 个, 属下一阶段规则扩充

## 分类补救(2026-06-13)

用户指出 `数据库/01八字命理` 分类流程有误, 已修复:

1. **顺序错误**: 先前先 `build_index` 后 `classify_01_sources`, 现已新增 `build_01_classified_index.py` 强制 `classify -> reconcile -> build_index`
2. **规则漏洞**: `authority_tier_for_classic` 曾让 `《添情命理解读滴天髓全部命例》` 因书名含「滴天髓」被判为 S/qishi_judge; 现 T3/T4 优先于经典名匹配
3. **实战命例**: 增补 T3 模式 `实战命理/实战断/段命`, C 级由 62 增至 69, S 级由 10 降至 9
4. **验证**: `test_case_file_with_classic_name_stays_experience_library` 通过
5. **索引重建完成**(2026-06-13T01:42:00): 143 文件 / 12354 chunks; `《添情...全部命例》` chunk 已为 C/case_reference/canJudge=0

## 下一阶段建议(仍本地)

1. 启动本地 RAG 服务后跑 `--rag` 版 val baseline, 观察 caseOverreach 是否下降
2. 子平真诠第二批 geju 节点扩充
3. 需要准确率基线时再跑 contest8_eval(LLM), 仍可不部署云端

## 状态结论

**完成**. 本地 smoke、val 判盘链基线、RAG 重建、metadata 抽测和文件分类清单均已完成; 云端部署明确跳过. Composer 按用户"仅本地"指令实施喵
